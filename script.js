document.addEventListener("DOMContentLoaded", () => {
    console.log("✨💖 ギャルポスタージェネレーター準備OK 💖✨");

    // PNG出力ボタン
    document.getElementById("download").addEventListener("click", () => {
        alert("PNG出力機能はまだ未実装！✨");
    });
});
